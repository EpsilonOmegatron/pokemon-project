$kind: http-request
name: Create Ability
method: POST
url: "{{baseUrl}}/api/abilities"
description: Create a new ability
order: 3000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Static",
      "description": "1/3 Chance of paralyzing the opponent if hit by a contact move"
    }
